<turbo-frame id="newStorageForm" class="d-none" {{ stimulus_target('storages', 'newInplace') }}>
{{ form_start(form) }}
	<div class="d-flex justify-content-end">
		{{ form_row(form.name) }}
		{{ form_row(form.save) }}
	</div>
{{ form_end(form) }}
</turbo-frame>