<turbo-frame id="folders" class="row m-2">
	<div class="col-12">
		<button type="button" class="btn btn-sm"><i class="fa fa-file-invoice"></i>neu</button>
		ddd
	</div>
	{% for folder in folders %}
		<div class="col-lg-3 col-md-4" id="folder_{{ folder.id }}" data-id="{{ folder.id }}">
			<div class="card">
				<div class="card-body">
					<h5><strong>{{ folder.name }}</strong></h5>
				</div>
			</div>
		</div>
	{% endfor %}
</turbo-frame>
