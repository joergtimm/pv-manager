{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} | Project index{% endblock %}

{% block main_content %}
    <div class="row bg-white">
        <div class="col-12">
            <hr>
            <h4><strong>Projekte - Liste</strong></h4>
            <hr>
        </div>
    </div>
<div class="row">
    <div class="col-12 d-flex justify-content-between p-3">
        <a href="#" class="btn btn-success me-3">+ Neu</a>
        <form class="app-search d-none d-md-block">
        <input type="text" class="form-control" placeholder="Search..." autocomplete="off" id="search-options" value="">
        </form>
    </div>
    <hr>
</div>

    <div class="row">
        {% for project in projects %}
        <div class="col-md-6 col-lg-4 p-2">
            <div class="card h-100 project-card border-1 border border-muted">
                <div class="card-body">
                    <div class="d-flex justify-content-between">
                        <p class="text-muted mb-4">Letzte Änderung: </p>
                        <i class="fa fa-star text-warning"></i>
                        <div><a href="{{ path('app_project_show', { id: project.id }) }}">...</a></div>
                    </div>
                    <div class="d-flex justify-content-start">
                        <img src="{{ asset('build/img/dropbox.png') }}" alt="Drop" height="50" class="me-2" />
                        <div>
                            <h5><strong>Hier viel Text unbd so</strong></h5>
                            <p>und hier ein wenig mehr Text</p>
                        </div>
                    </div>
                    <div class="d-flex justify-content-between">
                        <p>Tasks</p>
                        <div>
                            <i class="fa fa-list me-2"></i> 18/42
                        </div>
                    </div>
                    <div class="progress progress-bar-animated">
                        <div class="progress-bar bg-success" role="progressbar" aria-valuenow="34" aria-valuemin="0" aria-valuemax="100" style="width: 34%;">

                        </div>
                    </div>
                    <hr>
                    Bilder der Mitarbeiter
                </div>
            </div>
        </div>
        {% endfor %}
    </div>

{% endblock %}
